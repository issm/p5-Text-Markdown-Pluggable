This is Perl module Text::Markdown::Pluggable.

INSTALLATION

Text::Markdown::Pluggable installation is straightforward. If your CPAN shell is set up,
you should just be able to do

    % cpan Text::Markdown::Pluggable

Download it, unpack it, then build it as per the usual:

    % perl Makefile.PL
    % make && make test

Then install it:

    % make install

DOCUMENTATION

Text::Markdown::Pluggable documentation is available as in POD. So you can do:

    % perldoc Text::Markdown::Pluggable

to read the documentation online with your favorite pager.

issm
